import { ActiveFilesPipe } from './active-files.pipe';

describe('ActiveFilesPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveFilesPipe();
    expect(pipe).toBeTruthy();
  });
});
