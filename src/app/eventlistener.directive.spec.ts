import { EventlistenerDirective } from './eventlistener.directive';

describe('EventlistenerDirective', () => {
  it('should create an instance', () => {
    const directive = new EventlistenerDirective();
    expect(directive).toBeTruthy();
  });
});
